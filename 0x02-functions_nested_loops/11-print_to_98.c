#include <stdio.h>

int main(void)
{
	int n;

	void print_to_98(int x)
	{
		if (x > 98)
		{
		for (n = x; n >= 98; n--)
			printf("%d", n);
			if (n > 98)
				printf(", ");
			{
				printf("\n");
			else
				for (n = x; n <= 98; n++)
			{
				printf("%d", n);
				if (n < 98)
				printf(", ")
			}
				printf("\n");

			}

		}

	}

	print_to_98(0)
}

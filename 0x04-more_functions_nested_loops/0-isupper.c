#include "main.h"
/**
	* _isupper - prints uppercase characters.
	* @c: check the code.
	* Return: 1 otherwise 0.
	*/
int _isupper(int c)
{
	if
	(c >= 'A' && c <= 'z')
	return (1);
	else
	return (0);
}
